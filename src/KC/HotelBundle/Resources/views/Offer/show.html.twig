{% extends '::admin_base.html.twig' %}

{% block body -%}
    <h1>Oferta {{ entity.name }} </h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Opis</th>
                <td>{{ entity.discription }}</td>
            </tr>
            <tr>
                <th>Cena</th>
                <td>{{ entity.price }}</td>
            </tr>
        </tbody>
    </table>


        <a class="button" href="{{ path('admin_offer') }}">
            Powrót do listy
        </a>

        <a class="button" href="{{ path('admin_offer_edit', { 'id': entity.id }) }}">
            Edytuj
        </a>

        {{ form(delete_form) }}

{% endblock %}
