{% extends '::admin_base.html.twig' %}

{% block body -%}
    <h1>Edycja pracownika</h1>

    {{ form(edit_form) }}


    
        <a class="button" href="{{ path('admin_worker') }}">
            Powrót do listy
        </a>
    
    {{ form(delete_form) }}

{% endblock %}
