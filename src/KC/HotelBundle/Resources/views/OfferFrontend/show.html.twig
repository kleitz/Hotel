{% extends '::frontend_base.html.twig' %}

{% block body -%}
    <h1>Oferta {{ entity.name }} </h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Opis</th>
                <td>{{ entity.description }}</td>
            </tr>
            <tr>
                <th>Cena</th>
                <td>{{ entity.price }}</td>
            </tr>
        </tbody>
    </table>


        <a class="button" href="{{ path('offer') }}">
            Powrót do listy ofert
        </a>


{% endblock %}
